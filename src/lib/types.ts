export type Recipe = {
  title: string;
  slug: string;
  coverImage: string;
  timeMinutes: number;
  category: string;
  excerpt: string;
};

export type Article = {
  title: string;
  slug: string;
  coverImage: string;
  publishedAt: string;
  excerpt: string;
};
